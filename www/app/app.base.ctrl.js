(function() {

	var app = angular.module('starter.app');

/*	app.controller('AppController', ['$scope', '$location', '$ionicActionSheet', '$ionicSideMenuDelegate', function($scope, $location, $ionicActionSheet, $ionicSideMenuDelegate) {
	  $scope.groups = [];
	  var cat = [];
	  var sub_cat = [];
	  cat=['Mobile & Tablets','Automobiles','Mobile & Tablets','Automobiles','Mobile & Tablets','Automobiles'];
	  sub_cat=['...mobile','...automobile'];
	  for (var i=0; i<6; i++) {
		    $scope.groups[i] = {
		      name: cat[i],
		      items: []
		    };

		    for (var j=0; j<2; j++) {
		      $scope.groups[i].items.push(sub_cat[j]);
		    }
	  }
	  
  $scope.toggleGroup = function(group) {
    if ($scope.isGroupShown(group)) {
      $scope.shownGroup = null;
    } else {
      $scope.shownGroup = group;
    }
  };
  $scope.isGroupShown = function(group) {
    return $scope.shownGroup === group;
  };
  	
	}]);

})*/
});



